# agent-run

`agent-run` will discover, register, and run project commands while returning
bounded results and complete evidence for failures.

The command shell currently exposes help and the shared text and JSON output
contract. Command registration, detection, targeting, execution, and evidence
arrive in later chunks.

```sh
agent-run --help
agent-run --json
```
